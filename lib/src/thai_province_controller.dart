/// Controller for managing the selected Thai province, district, subdistrict, and zipcode.
class ThaiProvincesController {
  /// The ID of the selected province.
  int? provinceId;

  /// The ID of the selected district (amphure).
  int? amphureId;

  /// The ID of the selected subdistrict (tambon).
  int? tambonId;

  /// The zipcode of the selected subdistrict (tambon).
  int? zipcode;
}
